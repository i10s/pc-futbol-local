{
  "_comment": "Active community mirror. Both disk images and savestates are downloaded through this Cloudflare Worker by default — it adds the browser fingerprint (and, for disks, the ?k= signing token) the official hosts' WAF now requires. Override per-user with PCF_MIRROR / PCF_DISKS_BASE / PCF_ORIGIN_BASE.",
  "disks": "https://pcf-mirror.ifuentes.workers.dev",
  "origin": "https://pcf-mirror.ifuentes.workers.dev"
}
